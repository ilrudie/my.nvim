-- Keymaps are automatically loaded on the VeryLazy event
-- Default keymaps that are always set: https://github.com/LazyVim/LazyVim/blob/main/lua/lazyvim/config/keymaps.lua
-- Add any additional keymaps here

-- local map = vim.keymap.set
--
-- local unset = vim.keymap.del

-- These are things I use for tmux
-- unset("n", "<C-h>")
-- unset("n", "<C-j>")
-- unset("n", "<C-k>")
-- unset("n", "<C-l>")

-- Move to window using the <ctrl> hjkl keys
-- map("n", "<A-h>", "<A-w>h", { desc = "Go to Left Window", remap = true })
-- map("n", "<A-j>", "<A-w>j", { desc = "Go to Lower Window", remap = true })
-- map("n", "<A-k>", "<A-w>k", { desc = "Go to Upper Window", remap = true })
-- map("n", "<A-l>", "<A-w>l", { desc = "Go to Right Window", remap = true })
